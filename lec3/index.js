/*
var -> global scope, produces bug, found in pre-ES6 codebase, can be re-declared, can be updated, default value is `undefined`
const -> used to define a constant literal, must be declaration + initialisation, no default value
let -> modern ES6 feature, block-level scope, can not be re-declared, can be updated, no default value
*/
